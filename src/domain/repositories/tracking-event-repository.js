/* eslint-disable no-unused-vars */
class TrackingEventRepository {
  async registrar({ donationId, changedBy, fromStatus, toStatus }) { throw new Error('No implementado'); }
  async listarPorDonacion(donationId) { throw new Error('No implementado'); }
}

module.exports = TrackingEventRepository;
